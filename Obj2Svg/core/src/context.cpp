#include "editor_core/context.hpp"

// EditorContext is a plain aggregate for now (just LogSink). This
// translation unit is reserved for future context-specific helpers —
// e.g. factory functions or setup logic for the allocator/locale/cache
// fields once they're added (see context.hpp). Nothing to implement yet.

namespace editor_core {} // namespace editor_core
